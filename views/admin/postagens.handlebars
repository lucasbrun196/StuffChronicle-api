<h1>Nova postagem</h1>
<hr>
<a href="/admin/postagens/add"><button class="btn btn-success">Adicionar postagem</button></a>

{{#each postagens}}
<div class="card mt-4">
    <div class="card-body">
       <h2>{{titulo}}</h2>
       <small>{{slug}}</small>
       <h3>{{descircao}}</h3>
       <h4>{{conteudo}}</h4>
       <small>{{categoria.nome}}</small><br>
       <small>{{data}}</small><br>
       <a href="/admin/postagens/edit/{{_id}}"><button class="btn btn-success mt-4">Editar</button></a>
    </div>
</div>

{{else}}
{{/each}}